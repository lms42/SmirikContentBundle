{% extends 'SmirikCourseBundle:Admin:layout.html.twig' %}

{% block title %}{{ 'admin.content.edit.page_title'|trans }}{% endblock %}

{% block header %}
<h1>{{ entity.title }} <small>{{ 'admin.content.edit.title.comment'|trans }}</small></h1>
{% endblock %}

{% block body %}

{% form_theme edit_form 'SmirikAdminBundle:Form:fields.html.twig' %}

<form action="{{ path('admin_content_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }} class="form-horizontal">
  <fieldset>
    {{ form_widget(edit_form) }}
    <p>
        <button type="submit" class="btn btn-primary">{{ 'admin.content.action.save'|trans }}</button>
    </p>
  </fieldset>
</form>

<form action="{{ path('admin_content_delete', { 'id': entity.id }) }}" method="post">
<div class="well">
    {{ form_widget(delete_form) }}
		<a href="{{ path('admin_content') }}" class="btn">
		    {{ 'admin.content.action.back'|trans }}
		</a>
    <button type="submit" class="btn btn-danger">{{ 'admin.content.action.delete'|trans }}</button>
</div>
</form>

{% endblock %}
