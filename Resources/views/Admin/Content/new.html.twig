{% extends 'SmirikCourseBundle:Admin:layout.html.twig' %}

{% block title %}{{ 'admin.content.new.page_title'|trans }}{% endblock %}

{% block header %}
<h1>{{ 'admin.content.new.title'|trans }} <small>{{ 'admin.content.new.title.comment'|trans }}</small></h1>
{% endblock %}

{% block content %}

{% form_theme form 'SmirikAdminBundle:Form:fields.html.twig' %}

<form action="{{ path('admin_content_create') }}" method="post" {{ form_enctype(form) }} class="form-horizontal">
  <fieldset>
    {{ form_widget(form) }}
    <div class="well">
      <a href="{{ path('admin_content') }}" class="btn">
          {{ 'admin.content.action.back'|trans }}
      </a>
      <button type="submit" class="btn btn-primary">{{ 'admin.content.action.save'|trans }}</button>
    </div>
  </fieldset>
</form>


{% endblock %}
