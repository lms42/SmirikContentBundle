<h4>{{ 'Categories'|trans }}</h4>
<ul class="tags">
{% for category in categories %}
    <li><a href="{{ path('category_index', {'urlkey': category.urlkey }) }}">{{ category.title }}</a></li>
{% endfor %}
